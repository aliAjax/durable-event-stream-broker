# gRPC transport boundary

The production service reserves this package for generated protobuf adapters. The default build uses the HTTP transport and the domain ports remain independent of gRPC vendors.
